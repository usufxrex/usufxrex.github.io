===============================================
GITHUB PRIVATE REPOSITORIES SETUP GUIDE
===============================================

🔒 HOW TO SHOW PRIVATE REPOS ON YOUR WEBSITE

Step 1: Generate GitHub Personal Access Token
   1. Go to: https://github.com/settings/tokens
   2. Click "Generate new token (classic)"
   3. Give it a name: "Portfolio Website"
   4. Select scope: ✅ repo (Full control of private repos)
   5. Set expiration: No expiration (or your choice)
   6. Click "Generate token"
   7. COPY the token immediately (starts with ghp_...)
      ⚠️ You won't be able to see it again!

Step 2: Add Token to Your Website Code
   1. Open: index.html
   2. Find line: const GITHUB_TOKEN = '';
   3. Replace with: const GITHUB_TOKEN = 'ghp_yourTokenHere';
   4. Save the file

Step 3: Test Your Website
   1. Refresh your browser (Ctrl + F5)
   2. Go to Projects section
   3. You should now see private repos with 🔒 badge

===============================================
⚠️⚠️⚠️ CRITICAL SECURITY WARNING ⚠️⚠️⚠️
===============================================

🚨 DO NOT publish this token publicly!

If your website is PUBLIC (hosted on GitHub Pages, etc.):
   ❌ DO NOT add the token to index.html
   ❌ Anyone can view your page source
   ❌ They will steal your token
   ❌ They can access ALL your private repos

If your website is PRIVATE (password-protected server):
   ✅ Safe to add token
   ✅ Only you can access the website
   ✅ Private repos will show correctly

RECOMMENDATION for PUBLIC websites:
   - Keep GITHUB_TOKEN = '' (empty)
   - Only show public repos
   - OR: Build a backend API to hide the token

===============================================
WHAT HAPPENS WITH PRIVATE REPOS
===============================================

When someone clicks a private repo:
   ┌──────────────────────────────────┐
   │ 🔒 This is a private repository. │
   │                                   │
   │ To request access, contact me:    │
   │ ✉️ emonyousuf.bd@gmail.com       │
   │ 💬 Telegram: +8801708936687      │
   │ Or use Contact section           │
   └──────────────────────────────────┘

Private repos display:
   - 🔒 Red "Private" badge
   - 80% opacity (slightly faded)
   - No GitHub link
   - "Request Access" button
   - Hidden stars/forks count
   - Description still visible

Public repos display normally:
   - Full brightness
   - Clickable GitHub link
   - Stars and forks visible
   - Live demo link (if available)

===============================================
CURRENT STATUS
===============================================

Token Status: NOT SET (empty)
Result: Only PUBLIC repos will show
Security: ✅ SAFE for public websites

If you want private repos visible:
   → Follow Step 1 & 2 above
   → Only do this if website is password-protected!

===============================================
TO UPDATE CONTACT INFO IN POPUP
===============================================

The "Request Access" popup shows:
   - Email: emonyousuf.bd@gmail.com
   - Telegram: +8801708936687

To change this:
1. Open index.html
2. Search for: "Request Access"
3. Update the email/telegram in the alert message
4. Save and refresh

===============================================
TOKEN MANAGEMENT
===============================================

If token is compromised:
1. Go to: https://github.com/settings/tokens
2. Find your token
3. Click "Delete"
4. Generate a new token
5. Update index.html with new token

Token permissions needed:
   ✅ repo (to access private repos)
   ❌ admin:org (not needed)
   ❌ delete_repo (not needed)

===============================================
TROUBLESHOOTING
===============================================

Private repos not showing:
   ✓ Check token is added correctly
   ✓ Token must start with 'ghp_'
   ✓ Check token has 'repo' scope
   ✓ Clear cache: localStorage.removeItem('github_repos_cache');

Error "401 Unauthorized":
   → Token is invalid or expired
   → Generate new token

Error "403 Forbidden":
   → Rate limit exceeded
   → Wait 1 hour or use token

Repos showing but no private badge:
   → Make sure repos are actually private on GitHub
   → Check GitHub settings

===============================================
RECOMMENDATION
===============================================

For MAXIMUM SECURITY on public websites:

Option 1: Don't show private repos
   - Keep GITHUB_TOKEN = ''
   - Only public repos visible
   - 100% safe

Option 2: Build a backend
   - Create Node.js/Python backend
   - Store token on server
   - Website calls your API
   - Token never exposed to public

Option 3: Use environment variables (advanced)
   - Build process injects token
   - .env file (never committed)
   - Requires build system

===============================================
